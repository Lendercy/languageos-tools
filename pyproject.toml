[project]
name = "languageos-tools"
version = "0.1.0"
description = "Automation tools for LanguageOS"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]